# 项目协作规则 — esonare_SoundTherapyPro (心声冥想)

## 一、Memory（supermemory MCP）使用规则

本项目已接入 supermemory MCP（mcp-supermemory-ai），用于跨会话持久记忆。
注意：该 MCP 是全局配置，并非项目专属，所以所有读写都必须显式打上本项目的标签，
避免和你以后其他项目的记忆混在一起检索出来。

**项目标签（containerTag / project tag）：固定使用 `esonare-soundtherapy-pro`**
- 调用 supermemory 的任何工具（search / addMemory / recall 等）时，
  必须带上这个项目标签作为过滤条件或 tag 参数。
- 如果工具本身支持 `containerTag` 或类似的命名空间参数，优先用它而不是
  在内容文本里写项目名。

### 1. 任务开始前 — 必须先查
在执行任何具体编码/调试任务之前，**先调用 supermemory 搜索**，
查询内容应包含：
- 当前要处理的文件名 / 模块名 / bug 描述关键词
- 项目标签 `esonare-soundtherapy-pro`

目的：避免重复诊断已经定位过的问题，避免跟之前确认过的根因冲突。
如果搜索结果里有相关历史记录，先在回复里简要复述"根据已有记录，这个问题之前的结论是……"，
再继续，而不是从零分析。

### 2. 任务完成后 — 按需存，不要存流水账
完成一个**有实际结论**的任务后才存记忆，不是每次对话都存。

需要存的情况：
- 定位到一个 bug 的根本原因（不只是症状描述）
- 确认了一个修复方案并验证有效/无效
- 做出了一个会影响后续开发的架构/技术选型决策
- 排除了一个曾经怀疑但证伪的假设（避免以后又走回头路）

不需要存的情况：
- 单纯的代码格式调整、注释、reflow
- 还没验证过的猜测性假设
- 已经存在记忆里、没有新信息的重复内容

存储格式建议（简洁，便于以后检索）：
[esonare-soundtherapy-pro] <模块/文件名>: <一句话结论>

状态: 已确认根因 / 已修复待验证 / 已修复已验证 / 已排除的假设

细节: <2-3句，包含关键文件路径和具体机制>

### 3. 检索关键词建议
本项目目前的高频排查对象，搜索时可以用这些关键词命中历史记录：
- thumbnail / 缩略图错位 / SCENES 数组 / SceneItem
- switchSoundscape / download progress / 下载进度
- HomeScreen unmount / resourceLoadingChanged / subscribeDownload
- DownloadService / DownloaderService / useResourceDownloader

## 二、协作习惯（已有偏好，必须遵守）

1. **只做明确要求的事**。不要在没有被要求的情况下：
   - 顺手"优化"或重构没让你碰的代码
   - 主动做代码审查并罗列一堆建议
   - 修改和当前任务无关的文件

2. **单一目的的修改**。每次只针对一个明确问题做一处改动，改完等待用户测试反馈，
   再进行下一步，不要一次性打包多个改动。

3. 如果用户的指令前缀是 "Read-only, no changes" 或类似表述，
   严格只做分析/只读检查，不写入、不修改任何文件。

4. 涉及超大文件（如 AudioService.ts ~4000行）的跨文件综合分析，
   如果当前用的是本地模型（如 Qwable/Qwen3-Coder via Ollama）且力不从心
   （出现 Invalid API Response、输出失控等），应提示用户这类复杂诊断
   建议交给Claude 而不是继续用本地模型硬跑。

## 三、称呼规范

- 向用户汇报、回复消息时，一律称呼用户为"大哥"。

## 💡 MCP 协作与开发踩坑潜规则 (2026-06)

1. **codebase-memory 限制与变通**：`codebase-memory-mcp` 是纯代码图谱引擎，无法直接存储自由文本备注。后续如果遇到"不允许硬编码"、"架构解耦铁律"等元知识规范，必须在相关文件顶部添加 JSDoc 块并标记 `@architecture-constraint`，然后引导其重新 `index_repository`，让规范随 Module 节点属性一同被检索。


## 五、禁止使用 （2026-06-28 新增）

**绝对禁止使用  工具。**
无论任何场景——需求不明确、技术选型有歧义、存在多种可行方案——都禁止调用该工具请求用户确认或直接询问更多信息。
一旦需要做出判断，基于上下文自行推断最合理的做法并直接执行，直到任务完成为止。

## 六、边分析边执行（2026-06-28 新增）

**每轮回复必须同时包含至少一个工具调用。**

禁止只输出纯文字分析而不调用任何工具。遇到需要排查、定位、理解的问题，必须：
1. **先明确要做什么**（简短说明意图），
2. **立刻执行对应工具调用**（read / search / exec 等实际干活），
3. **拿到结果后再进一步分析或下结论。**

不要在回复里先写完大段文字解释，然后再去调工具。如果当前请求不需要任何工具就可以完成（比如纯闲聊、确认已知信息），可以只输出文字。