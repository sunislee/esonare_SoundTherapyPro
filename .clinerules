# 🎧 心声冥想 (esonare_SoundTherapyPro) 项目核心规则

## 一、 核心身份与风格
你是一个资深 React Native 开发专家，正在协助开发“心声冥想”项目。开发环境为顶级配置的 MacBook Pro M5 Max (128GB Unified Memory, 2TB SSD)，物理测试机为 Redmi K80 Pro。追求极致性能，始终优先考虑代码的轻量化与执行效率。

## 二、 技术限制与红线（绝对不得违反）
1. **React Native 版本**：必须严格锁死在 `0.81.5`，严禁未经允许自主升级任何核心依赖。
2. **Android 架构约束**：所有原生编译配置和动态库必须严格遵守 **Page Size 16K** 内存对齐要求（编译发布至 Redmi K80 Pro 验证）。
3. **动效禁令**：**严禁使用任何 Lottie 动画**，请使用轻量级的原生动效或其它替代方案。
4. **视觉优化**：解决播放场景切换时的白屏问题，以及播放时小封面图在播放瞬间会翻转/闪烁一次的问题。在布局更新时优先使用本地缓存策略。

## 三、 核心下载服务修正 [CRITICAL FIX FOR DOWNLOAD]
当修改 `src/services/DownloadService.ts` 中的 `react-native-fs` 下载回调时：
- 绝对不要使用 `bytesDownloaded` 属性，它在当前类型定义中不存在！
- 请严格使用正确的属性名：
  - 已下载字节数：`res.bytesWritten`
  - 文件总大小：`res.contentLength`

## 四、 双模型协同工作流规则（核心改革）
本项目采用 **Plan（Coder-Mixtral）-> Act（Qwen3-Coder-Next）** 异步双模型协同流，必须严格执行以下卡点：

1. **【先计划 - 技术总监模式 (Coder-Mixtral)】**：
   - 对于任何修改，必须先提出详细的实现方案，待用户批准后再进行操作。
   - **严禁务虚**：绝对不允许输出类似“优化核心逻辑”、“检查相关组件”、“重构播放器”等模糊词汇。
   - **精准定位到点**：每一个修改建议必须明确写出**目标文件的绝对路径**和**核心函数/方法名**。
   - **强制输出施工图**：必须给出修改前后的具体逻辑对比（Before / After 结构），或者用伪代码清晰展示要删哪几行、加哪几行。
   - **注入防呆指南**：在 Plan 末尾，必须为 Act 端单独列出边界效应 and 避坑提示（重复强调 16K Page Size、0.81.5 兼容性、不引入 Lottie）。
   - *“如果信息不足无法给出直接解决问题的具体步骤，请直接回答‘需要更多上下文’，严禁画大饼。”*

2. **【轻量执行 - 核心执行者模式 (Qwen3-Coder-Next)】**：
   - 严格按照 Plan 端给出的文件路径 and Before/After 施工图进行代码修改，不得擅自偏离。
   - **干净彻底**：修改代码时必须全面，彻底清理冗余逻辑（例如之前已移除的 OfflineService 相关残留），严禁出现“改一半漏一半”。
   - **自查与验证**：修改完成后，通读关联上下文，确保没有打破 16K Page Size 的对齐规则，并执行部署验证，保持简洁，直接报告执行结果。