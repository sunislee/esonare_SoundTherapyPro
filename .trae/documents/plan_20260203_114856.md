# 重新编译英文Debug包并部署到真机

## 步骤1：验证Gradle配置

* 检查 `SoundTherapyApp/android/gradle/wrapper` 目录，确认 `gradle-wrapper.jar` 文件已存在

* 执行：`cd SoundTherapyApp/android && chmod +x gradlew && ./gradlew --version`

* 目的：确保 Gradle 包装器能正常运行，验证环境配置

## 步骤2：确认英文配置

* 检查 `src/i18n/index.ts` 文件，确认 `lng` 和 `fallbackLng` 已设置为 `'en'`

* 验证结果：配置已正确设置，无需修改

## 步骤3：启动Metro Bundler

* 执行：`npx react-native start --reset-cache`（在后台运行）

* 目的：清除Metro缓存，确保新的代码变更能够被正确识别

* 处理端口冲突：如果遇到端口8081被占用的情况，自动kill旧进程并重新启动

## 步骤4：重新编译并部署Debug版本

* 在 `SoundTherapyApp` 目录下执行：`npx react-native run-android --variant=debug`

* 目的：重新编译英文Debug版本并部署到连接的Android真机

* 验证：确保看到编译进度条在运行，特别是 `> Task :app:installDebug` 步骤

## 步骤5：验证部署结果

* 确认应用成功安装到Android真机

* 验证应用启动时显示英文界面

* 确认Metro Bundler正常运行，支持热更新

## 预期结果

* 应用成功编译并部署到Android真机

* 应用启动时显示英文界面

* Metro Bundler正常运行，支持代码变更的实时更新

* 编译过程顺利完成，无错误