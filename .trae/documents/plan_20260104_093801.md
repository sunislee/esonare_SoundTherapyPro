## 视觉与听感优化计划

### 1. 文案切换动画 (300ms 淡入淡出)
**位置**: TherapyStatus 组件 (App.js:78-186)

**修改内容**:
- 给 `<Text>` 组件添加 `AnimatedOpacity` 效果
- 在文字切换时加入 fadeIn/fadeOut 动画
- 保持现有的水平滚动作为主动画，opacity 作为辅助

### 2. 粉噪"软着陆"修复
**位置**: switchScene 函数 (App.js:456-488)

**问题诊断**:
- 当前问题是 `TrackPlayer.reset()` 在淡出完成前就被调用
- 需要在 `fadeOutAndStop` 完成后使用 Promise 通知主流程

**修改内容**:
- 将 `fadeOutAndStop` 改为返回 Promise
- 等待淡出完成后才执行 `TrackPlayer.reset()`
- 确保 5秒淡出到 0.1 后再切换场景

### 3. 呼吸环色彩同步
**位置**: SCENES 配置 (App.js:188-238)

**修改颜色**:
- α波：`#4a90e2` (深蓝色)
- 粉噪：`#f8bbd0` (暖粉色)
- 双耳：`#7e57c2` (紫色)

### 实现顺序
1. 先修复粉噪淡出逻辑（核心功能）
2. 再调整呼吸环颜色
3. 最后添加文案淡入淡出动画

修改完成后重新构建测试。