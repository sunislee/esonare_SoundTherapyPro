## "清晨河流"场景更新计划

### 修改内容

#### 1. 核心引用替换
将 "清晨河流" 场景的音频路径更新为：
```javascript
audio: require('./assets/audio/tracks/morning_river_zen.m4a')
```

#### 2. UI 意境同步
- **副标题**：`'晨曦鸟语，流水潺湲'` (原: '河边晨曦')
- **主题色**：`'#a8e6cf'` (清新绿)

#### 3. 播放性能确保
- m4a 格式文件会自动使用 LC-AAC 编解码器解码
- 保持现有的异步音量爬升逻辑 (0.3 -> 0.5)
- 保持"秒开"的响应速度

### 修改位置
文件: `App.js`
位置: SCENES 数组中的 `morning_river` 对象 (约第360-380行)

### 修改后验证
1. 底部菜单显示 "清晨" 按钮
2. 点击后呼吸环显示清新绿色 (#a8e6cf)
3. 播放 "晨曦鸟语，流水潺湲" 意境音效
4. 音量在 15 秒内从 30% 平滑爬升到 50%