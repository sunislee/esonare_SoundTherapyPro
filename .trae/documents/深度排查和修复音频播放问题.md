## 深度排查和修复音频播放问题

### 1. 增强 handleToggle 函数
- 在 `await TrackPlayer.play()` 之后添加 `const state = await TrackPlayer.getState()` 并 console.log
- 在 `play()` 之前显式调用 `await TrackPlayer.setVolume(1.0)` 确保音量没被重置
- 添加 200ms setTimeout 延迟在 add 之后再执行 play

### 2. 完善后台服务
- service.js 已有基本事件监听，保持不变

### 3. 检查并修正文件后缀名
- res/raw/ 中实际文件：pink_noise.mp3（不是 pink_noise_zen.m4a）
- res/raw/ 中实际文件：没有 alpha_wave.m4a，只有 alpha_wave.mp3
- 修正 App.js 中的文件名以匹配实际文件

### 4. 重新构建和测试
- 执行 gradlew assembleDebug
- 安装到真机
- 查看日志确认播放状态