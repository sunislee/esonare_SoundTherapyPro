## 执行计划

### 1. 确认 App.js SCENES 配置
- 检查 SCENES 数组是否已经使用 .mp3 文件名

### 2. 确认 handleToggle 双重重置逻辑
- 确保 playScene 函数中有两次 TrackPlayer.reset()
- 确保有 300ms 延迟

### 3. 强制构建
- 运行 `cd android && ./gradlew assembleDebug`
- 忽略仓库警告，直接生成 APK