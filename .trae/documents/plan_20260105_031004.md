1. 在 audioProcessing 回调函数起始位置添加 LOGD 日志，确认回调是否执行
2. 修改 nativeInitialize 函数，限制缓冲区大小不超过 1024
3. 在 nativeStartAudio 函数中打印 audioIO->start() 的返回值
4. 修改 audioProcessing 回调函数，添加简单的正弦波测试代码，绕过复杂逻辑
5. 重新构建并运行应用，观察日志输出和是否有声音
6. 根据测试结果调整代码

