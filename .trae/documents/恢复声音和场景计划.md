# 恢复声音和场景计划

## 1. 清空冗余逻辑
- 修改App.js中的`changeScene`函数，移除"当前模式："这样的差异化状态文本
- 确保所有场景在状态显示上是平等的，统一使用"当前场景：{scene.title}"格式

## 2. 重构平行场景
- scenes数组已经包含了所有7个场景，它们是平等的，无需修改
- 确保点击任何场景按钮都会直接触发SuperpoweredModule.playScene()

## 3. 标准编译安装
- 停止当前所有运行的adb logcat命令
- 运行`npx react-native run-android`让React Native自动处理构建和安装

## 4. 修复静音问题
- 检查Java层的playScene方法，确保它在切换场景时先stop上一个音频
- 确保playScene方法中强制设置了masterVolume = 1.0f
- 验证generateAudio方法根据currentAudioMode正确生成音频

## 5. 测试所有场景
- 安装完成后，在模拟器中测试所有7个场景
- 确保切换场景时声音正常播放，没有重叠
- 确保toggleAudio按钮能正确开关音频